interface:
  display_name: "Technical Writing"
  short_description: "Plan, draft and publish technical prose."
  default_prompt: "Use $technical-writing to produce a publication-ready technical explanation."
policy:
  allow_implicit_invocation: true
